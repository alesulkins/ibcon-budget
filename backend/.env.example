DB_HOST=localhost
DB_PORT=5432
DB_USER=ibcon
DB_PASSWORD=ibcon_secret
DB_NAME=ibcon_budget
DB_SSLMODE=disable

JWT_SECRET=change_me_to_a_long_random_secret_32chars
JWT_EXPIRY_HOURS=60

SERVER_PORT=8080
GIN_MODE=debug

# Адреса, которым разрешены запросы из браузера (через запятую).
# Пусто — только свой адрес: и в разработке, и на стенде фронтенд
# отдаётся с того же хоста, CORS ему не нужен.
ALLOWED_ORIGINS=
