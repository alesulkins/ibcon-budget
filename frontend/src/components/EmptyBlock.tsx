/**
 * Пустое состояние блока — одна подпись на весь проект.
 *
 * Раньше у каждого блока был свой текст («Покупок нет», «Офисов нет»,
 * «Позиций нет»), и они разъезжались по смыслу и по месту. Теперь
 * подпись одна и стоит по центру блока.
 */
export default function EmptyBlock() {
  return (
    <div style={{
      textAlign: 'center',
      color: '#999',
      fontSize: 12,
      padding: '20px 0',
    }}>
      пока тут пусто
    </div>
  );
}
