// Пустое состояние блока — одна подпись на весь проект.
export default function EmptyBlock() {
  return (
    <div style={{
      textAlign: 'center',
      color: '#999',
      fontSize: 12,
      padding: '20px 0',
    }}>
      пока тут пусто
    </div>
  );
}
